import 'package:flutter/material.dart';

class RegisterAsPartnerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Register as Partner'),
      ),
      body: Center(
        child: Text('Register as Partner Page'),
      ),
    );
  }
}
